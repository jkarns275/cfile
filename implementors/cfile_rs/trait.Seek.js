(function() {var implementors = {};
implementors["cfile_rs"] = ["impl <a class="trait" href="cfile_rs/trait.Seek.html" title="trait cfile_rs::Seek">Seek</a> for <a class="struct" href="cfile_rs/struct.CFile.html" title="struct cfile_rs::CFile">CFile</a>",];

            if (window.register_implementors) {
                window.register_implementors(implementors);
            } else {
                window.pending_implementors = implementors;
            }
        
})()
